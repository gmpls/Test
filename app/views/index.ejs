<!DOCTYPE html>
<html>

<head>
	<!-- Use the 'title' property to render a page title  -->
	<title>
		<%= title %>
	</title>
</head>

<body>
	<!-- Load the static image file  -->
	<img src="img/logo.png" alt="Logo">

	<!-- Use the 'title' property to render a title element  -->
	<h1>
		<%= title %>
	</h1>

	<ul>
		<% var myArray = [{name:"Volvo", color:"white"},{name:"Ford", color:"green"}] %>
			<% myArray.forEach(function(myElement) { %>
				<li>
					<%= myElement.name %> -
						<%= myElement.color %>
				</li>
				<% }); %>
	</ul>
</body>

</html>